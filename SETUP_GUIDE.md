# 📋 دليل إعداد لوحة التحكم والأمان

## الخطوات المطلوبة لإكمال الإعداد

### 1️⃣ تثبيت المكتبات الجديدة
افتح تيرمنال جديد (غير الذي يشغل `npm run dev`) وشغل:

```bash
npm install
```

هذا الأمر سيقوم بتثبيت جميع المكتبات الجديدة التي أضفناها:
- `@heroicons/react` - الأيقونات للوحة التحكم
- `@upstash/ratelimit` و `@upstash/redis` - للحماية من الطلبات الكثيرة
- `nodemailer` - لإرسال إيميلات التحقق
- `winston` - لتسجيل الأحداث
- `zod` - للتحقق من المدخلات

---

### 2️⃣ تحديث قاعدة البيانات
بعد تثبيت المكتبات، شغل:

```bash
npx prisma migrate dev --name add_security_fields
```

هذا سيضيف الحقول الجديدة للمستخدمين:
- `role` (USER, MODERATOR, ADMIN)
- `status` (ACTIVE, SUSPENDED, BANNED)
- `verificationToken` و `verificationTokenExpiry`
- `lastLoginAt`

---

### 3️⃣ إعداد متغيرات البيئة (.env)
افتح ملف `.env` وأضف هذه المتغيرات:

```env
# قاعدة البيانات (موجودة مسبقاً)
DATABASE_URL="file:./dev.db"

# NextAuth - مهم جداً! استخدم أمر قوي
# لتوليد مفتاح آمن: openssl rand -base64 32
NEXTAUTH_SECRET="ضع_هنا_مفتاح_قوي_جداً"
NEXTAUTH_URL="http://localhost:3000"

# Upstash Redis (للحماية من الطلبات الكثيرة)
# اتركها فارغة الآن - يمكن إعدادها لاحقاً
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# إعدادات البريد الإلكتروني (اختياري - للتحقق من الإيميل)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your_email@gmail.com"
SMTP_PASS="your_app_password"
SMTP_FROM="noreply@promptlibrary.com"
```

**ملاحظة مهمة:** إذا تركت `UPSTASH_REDIS_REST_URL` فارغة، سيعمل النظام بدون Rate Limiting (مؤقتاً).

---

### 4️⃣ إنشاء مستخدم مدير
لاختبار لوحة التحكم، تحتاج مستخدم بصلاحيات مدير:

**الطريقة 1: باستخدام Prisma Studio**
```bash
npx prisma studio
```
ثم:
1. افتح جدول `User`
2. اختر مستخدم موجود
3. غيّر حقل `role` من `USER` إلى `ADMIN`
4. احفظ التغييرات

**الطريقة 2: باستخدام SQL مباشرة**
افتح تيرمنال وشغل:
```bash
npx prisma db execute --stdin
```
ثم الصق:
```sql
UPDATE User SET role = 'ADMIN' WHERE email = 'your_email@example.com';
```
(استبدل `your_email@example.com` بإيميل المستخدم)

---

### 5️⃣ إعادة تشغيل الخادم
بعد تثبيت المكتبات وتحديث قاعدة البيانات:

1. أوقف `npm run dev` الحالي (Ctrl+C)
2. شغله من جديد: `npm run dev`

---

### 6️⃣ اختبار لوحة التحكم

افتح المتصفح وجرب:

1. **الصفحة الرئيسية:** `http://localhost:3000`
2. **لوحة التحكم:** `http://localhost:3000/admin`
   - إذا لم تكن مدير، سيتم توجيهك للصفحة الرئيسية
   - سجل دخول بحساب المدير الذي أنشأته

**صفحات لوحة التحكم المتاحة:**
- `/admin` - لوحة المعلومات (Dashboard)
- `/admin/users` - إدارة المستخدمين
- `/admin/prompts` - إدارة البرومبتات
- `/admin/categories` - عرض الفئات
- `/admin/analytics` - الإحصائيات المتقدمة
- `/admin/settings` - الإعدادات
- `/admin/logs` - السجلات
- `/admin/backup` - النسخ الاحتياطي
- `/admin/notifications` - الإشعارات

---

## ✅ قائمة التحقق

- [ ] تثبيت المكتبات (`npm install`)
- [ ] تحديث قاعدة البيانات (`npx prisma migrate dev`)
- [ ] إضافة متغيرات البيئة في `.env`
- [ ] إنشاء مستخدم مدير
- [ ] إعادة تشغيل الخادم
- [ ] اختبار لوحة التحكم

---

## 🔧 حل المشاكل الشائعة

### المشكلة: لا يمكن الوصول للوحة التحكم
**الحل:** تأكد من أن المستخدم الذي سجلت دخوله لديه `role = 'ADMIN'`

### المشكلة: أخطاء في الأيقونات
**الحل:** تأكد من تثبيت `@heroicons/react` بنجاح

### المشكلة: Rate Limiting لا يعمل
**الحل:** هذا طبيعي إذا لم تعد Upstash Redis. النظام سيعمل بدونه مؤقتاً.

### المشكلة: إرسال الإيميلات لا يعمل
**الحل:** تحتاج إعداد SMTP. يمكنك تخطي هذا الآن - التسجيل سيعمل بدون التحقق من الإيميل.

---

## 📝 ملاحظات إضافية

1. **الأمان:** لا تنسى تغيير `NEXTAUTH_SECRET` لمفتاح قوي قبل النشر
2. **قاعدة البيانات:** حالياً تستخدم SQLite - للإنتاج استخدم PostgreSQL أو MySQL
3. **Rate Limiting:** للإنتاج، سجل حساب مجاني في [Upstash](https://upstash.com) واحصل على المفاتيح
4. **الإيميلات:** للإنتاج، استخدم خدمة مثل SendGrid أو AWS SES

---

## 🎯 الخطوات التالية (بعد التشغيل)

1. اختبر جميع صفحات لوحة التحكم
2. جرب تعديل دور مستخدم من USER إلى MODERATOR
3. جرب حظر/تفعيل مستخدم
4. جرب حذف برومبت
5. راجع صفحة Analytics لرؤية الإحصائيات

---

**هل واجهت أي مشكلة؟ أخبرني وسأساعدك في حلها!** 🚀
