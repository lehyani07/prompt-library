# 🚀 دليل سريع للوصول إلى لوحة التحكم

## 📍 روابط مهمة

- **لوحة التحكم:** http://localhost:3000/admin
- **تسجيل الدخول:** http://localhost:3000/auth/signin

---

## 🔑 بيانات الدخول الافتراضية

بعد تشغيل `npx prisma db seed`:

**البريد:** `admin@example.com`  
**كلمة المرور:** `admin123`

---

## ⚡ طريقة سريعة لإنشاء مستخدم مدير

### الطريقة الأسهل (موصى به):

```bash
npm run create-admin your-email@example.com your-password "Your Name"
```

**مثال:**
```bash
npm run create-admin admin@mydomain.com MySecurePass123 "مدير النظام"
```

### أو بدون npm script:

```bash
npx ts-node --compiler-options '{"module":"CommonJS"}' scripts/create-admin.ts your-email@example.com your-password "Your Name"
```

---

## 📋 الخطوات الكاملة

### 1. تحديث قاعدة البيانات
```bash
npx prisma migrate dev
npx prisma generate
```

### 2. إنشاء مستخدم مدير
```bash
npm run create-admin admin@example.com admin123
```

### 3. تشغيل الخادم
```bash
npm run dev
```

### 4. تسجيل الدخول
1. افتح: http://localhost:3000/auth/signin
2. أدخل البريد وكلمة المرور
3. اضغط "Sign In"

### 5. الوصول للوحة التحكم
افتح: http://localhost:3000/admin

---

## 📂 صفحات لوحة التحكم

- `/admin` - لوحة المعلومات
- `/admin/users` - إدارة المستخدمين
- `/admin/prompts` - إدارة البرومبتات
- `/admin/categories` - الفئات
- `/admin/analytics` - الإحصائيات
- `/admin/settings` - الإعدادات
- `/admin/logs` - السجلات
- `/admin/backup` - النسخ الاحتياطي
- `/admin/notifications` - الإشعارات

---

## ⚠️ ملاحظات مهمة

1. **غير كلمة المرور الافتراضية** في بيئة الإنتاج
2. **تأكد من تسجيل الدخول** قبل الوصول للوحة التحكم
3. **المستخدم يجب أن يكون لديه `role = 'ADMIN'`**

---

للمزيد من التفاصيل، راجع ملف `ADMIN_ACCESS.md`





