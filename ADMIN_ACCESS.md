# 🔐 دليل الوصول إلى لوحة التحكم

## 📍 رابط لوحة التحكم

**لوحة التحكم:** `http://localhost:3000/admin`

**صفحة تسجيل الدخول:** `http://localhost:3000/auth/signin`

---

## 🔑 بيانات الدخول الافتراضية

بعد تشغيل سكريبت البذور (`seed`)، يمكنك استخدام:

**البريد الإلكتروني:** `admin@example.com`  
**كلمة المرور:** `admin123`

> ⚠️ **تحذير:** هذه بيانات افتراضية للاختبار فقط. يجب تغييرها في بيئة الإنتاج!

---

## 🚀 طريقة إنشاء مستخدم مدير

### الطريقة 1: استخدام السكريبت المخصص (موصى به)

```bash
npx ts-node --compiler-options '{"module":"CommonJS"}' scripts/create-admin.ts your-email@example.com your-password "Your Name"
```

**مثال:**
```bash
npx ts-node --compiler-options '{"module":"CommonJS"}' scripts/create-admin.ts admin@mydomain.com MySecurePass123 "مدير النظام"
```

### الطريقة 2: استخدام Prisma Studio

1. افتح Prisma Studio:
```bash
npx prisma studio
```

2. افتح المتصفح على `http://localhost:5555`
3. اختر جدول `User`
4. إما:
   - **إنشاء مستخدم جديد:** اضغط "Add record" وأدخل:
     - `email`: بريدك الإلكتروني
     - `name`: اسمك
     - `password`: كلمة مرور مشفرة (استخدم السكريبت أعلاه لتشفيرها)
     - `role`: `ADMIN`
   - **تعديل مستخدم موجود:** اختر مستخدم وغيّر `role` إلى `ADMIN`

### الطريقة 3: استخدام SQL مباشرة

```bash
npx prisma db execute --stdin
```

ثم الصق (استبدل القيم):
```sql
-- أولاً، احصل على كلمة مرور مشفرة من السكريبت أعلاه
-- ثم استخدم:
UPDATE User SET role = 'ADMIN' WHERE email = 'your-email@example.com';
```

---

## 📋 خطوات الوصول الكاملة

### 1. تأكد من تشغيل قاعدة البيانات

```bash
npx prisma migrate dev
npx prisma generate
```

### 2. إنشاء مستخدم مدير

استخدم إحدى الطرق أعلاه.

### 3. تشغيل الخادم

```bash
npm run dev
```

### 4. تسجيل الدخول

1. افتح المتصفح على: `http://localhost:3000/auth/signin`
2. أدخل البريد الإلكتروني وكلمة المرور
3. اضغط "Sign In"

### 5. الوصول للوحة التحكم

بعد تسجيل الدخول، افتح: `http://localhost:3000/admin`

---

## 📂 صفحات لوحة التحكم المتاحة

- `/admin` - لوحة المعلومات الرئيسية
- `/admin/users` - إدارة المستخدمين
- `/admin/prompts` - إدارة البرومبتات
- `/admin/categories` - عرض الفئات
- `/admin/analytics` - الإحصائيات المتقدمة
- `/admin/settings` - الإعدادات
- `/admin/logs` - السجلات
- `/admin/backup` - النسخ الاحتياطي
- `/admin/notifications` - الإشعارات

---

## ⚠️ حل المشاكل

### المشكلة: لا يمكن الوصول للوحة التحكم

**الحل:**
1. تأكد من أن المستخدم لديه `role = 'ADMIN'`
2. تأكد من تسجيل الدخول أولاً
3. تحقق من أن الخادم يعمل

### المشكلة: كلمة المرور لا تعمل

**الحل:**
- كلمات المرور يجب أن تكون مشفرة بـ bcrypt
- استخدم السكريبت `create-admin.ts` لإنشاء مستخدم بكلمة مرور مشفرة بشكل صحيح

### المشكلة: لا يوجد مستخدمون في قاعدة البيانات

**الحل:**
```bash
# تشغيل سكريبت البذور
npx prisma db seed
```

أو:
```bash
npx ts-node --compiler-options '{"module":"CommonJS"}' prisma/seed.ts
```

---

## 🔒 الأمان

1. **غيّر كلمة المرور الافتراضية** فوراً في بيئة الإنتاج
2. **استخدم كلمات مرور قوية** (8+ أحرف، أرقام، رموز)
3. **لا تشارك بيانات الدخول** مع أشخاص غير مصرح لهم
4. **راجع السجلات** بانتظام في `/admin/logs`

---

## 📞 المساعدة

إذا واجهت أي مشكلة:
1. راجع ملف `SETUP_GUIDE.md` للتفاصيل الكاملة
2. تحقق من سجلات الخادم في Terminal
3. تأكد من تحديث قاعدة البيانات: `npx prisma migrate dev`








